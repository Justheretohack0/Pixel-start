# Pixel Start – Build Instructions

## Requirements
- Node.js (v18 or newer recommended)
- npm

## Install dependencies
npm install

## Build extension files
npm run build

The production build will be created in the dist/ folder.

The contents of dist/ are copied into the Firefox extension directory
and packaged as pixel-start.xpi.
